import Location from '../engine/Location';

const insidegarage = new Location()
    .setId("Inside the Garage")
    .setDesc("Inside the garage." + 
    "\n\nThe only exit is back through the door.");

export default insidegarage;
