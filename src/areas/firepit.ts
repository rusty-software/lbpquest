import Location from '../engine/Location';

const firepit = new Location()
    .setId("Firepit")
    .setDesc("The firepit is ringed... A door to the south leads back inside the house, and the pool is to the east.");

export default firepit;